/* THIS FILE WAS GENERATED FOR PAYLOAD — the GraphQL endpoint (/cms-api/graphql). */
import config from "@payload-config";
import { GRAPHQL_POST } from "@payloadcms/next/routes";

export const POST = GRAPHQL_POST(config);
