// Re-export the shared Open Graph image so X/Twitter cards use the same
// branded 1200x630 artwork instead of falling back to a generic preview.
export { default, alt, size, contentType } from "./opengraph-image";
