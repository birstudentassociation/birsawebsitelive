# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/.next-verify/
/out/

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# testing
/coverage
/playwright-report
/test-results
/.playwright

# Lighthouse CI writes reports and its run manifest here whenever `npm run perf`
# is run locally. CI uploads its own copies, so these are never worth committing.
/.lighthouseci

.vercel
.env*

# Scratch workspace for agent-driven development runs (ledgers, briefs, reports).
# Never part of the repo: the git history is the durable record.
.superpowers/
