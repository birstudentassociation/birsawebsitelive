# Wave 6B diff report: TypeScript content modules -> Sanity documents

Generated by `scripts/migrate-modules.mjs`. Id deriver: lib/migration/ids.ts (export "documentId"). 30 document(s) written to `docs/migration/modules.ndjson`.

This is a diff report, not a success report: most of the fields named below are gaps, not migrations. See `docs/migration/modules.md` for why.

## Regulations (`content/activity/regulations/**` -> `regulation`)

| File                              | Outcome                                                                                                      |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `discipline-2568/chapter1.ts`     | not migrated (provisions/sections only, folded into regulation:discipline-2568's single sections gap)        |
| `discipline-2568/chapter2.ts`     | not migrated (provisions/sections only, folded into regulation:discipline-2568's single sections gap)        |
| `discipline-2568/chapter3.ts`     | not migrated (provisions/sections only, folded into regulation:discipline-2568's single sections gap)        |
| `discipline-2568/chapter4.ts`     | not migrated (provisions/sections only, folded into regulation:discipline-2568's single sections gap)        |
| `discipline-2568/doc.ts`          | not migrated (assembly module for discipline-2568)                                                           |
| `discipline-2568/meta.ts`         | migrated: title/slug/effectiveDate of regulation:discipline-2568                                             |
| `discipline-2568/prelim.ts`       | not migrated (provisions/sections only, folded into regulation:discipline-2568's single sections gap)        |
| `discipline-2568/transitional.ts` | not migrated (provisions/sections only, folded into regulation:discipline-2568's single sections gap)        |
| `documents.ts`                    | not migrated (assembly module for political-science-2565)                                                    |
| `index.ts`                        | not migrated (barrel)                                                                                        |
| `meta.ts`                         | migrated: title/slug/effectiveDate of regulation:political-science-2565                                      |
| `part01.ts`                       | not migrated (provisions/sections only, folded into regulation:political-science-2565's single sections gap) |
| `part02.ts`                       | not migrated (provisions/sections only, folded into regulation:political-science-2565's single sections gap) |
| `part03.ts`                       | not migrated (provisions/sections only, folded into regulation:political-science-2565's single sections gap) |
| `part04.ts`                       | not migrated (provisions/sections only, folded into regulation:political-science-2565's single sections gap) |
| `part05.ts`                       | not migrated (provisions/sections only, folded into regulation:political-science-2565's single sections gap) |
| `part06.ts`                       | not migrated (provisions/sections only, folded into regulation:political-science-2565's single sections gap) |
| `part07.ts`                       | not migrated (provisions/sections only, folded into regulation:political-science-2565's single sections gap) |
| `part08.ts`                       | not migrated (provisions/sections only, folded into regulation:political-science-2565's single sections gap) |
| `part09.ts`                       | not migrated (provisions/sections only, folded into regulation:political-science-2565's single sections gap) |
| `part10.ts`                       | not migrated (provisions/sections only, folded into regulation:political-science-2565's single sections gap) |
| `part11.ts`                       | not migrated (provisions/sections only, folded into regulation:political-science-2565's single sections gap) |
| `part12.ts`                       | not migrated (provisions/sections only, folded into regulation:political-science-2565's single sections gap) |
| `preliminary.ts`                  | not migrated (provisions/sections only, folded into regulation:political-science-2565's single sections gap) |
| `types.ts`                        | not migrated (types only)                                                                                    |
| `university-2563/doc.ts`          | not migrated (assembly module for university-2563)                                                           |
| `university-2563/meta.ts`         | migrated: title/slug/effectiveDate of regulation:university-2563                                             |
| `university-2563/prelim.ts`       | not migrated (provisions/sections only, folded into regulation:university-2563's single sections gap)        |
| `university-2563/title1.ts`       | not migrated (provisions/sections only, folded into regulation:university-2563's single sections gap)        |
| `university-2563/title2.ts`       | not migrated (provisions/sections only, folded into regulation:university-2563's single sections gap)        |
| `university-2563/title3a.ts`      | not migrated (provisions/sections only, folded into regulation:university-2563's single sections gap)        |
| `university-2563/title3b.ts`      | not migrated (provisions/sections only, folded into regulation:university-2563's single sections gap)        |
| `university-2563/title4.ts`       | not migrated (provisions/sections only, folded into regulation:university-2563's single sections gap)        |
| `university-2563/title5.ts`       | not migrated (provisions/sections only, folded into regulation:university-2563's single sections gap)        |
| `university-2563/transitional.ts` | not migrated (provisions/sections only, folded into regulation:university-2563's single sections gap)        |

Every regulation document received a stub (title, slug, and effectiveDate where the source's `made` line parsed): `regulation-discipline-2568`, `regulation-political-science-2565`, `regulation-university-2563`.

## Calendar (`content/calendar/events.ts`) — not migrated, assessment only

No `event` documents were created from this file. Every entry is a date marker pointing at a news/event post by slug, not standalone event content (no summary, category, location or body in the source); see `lib/migration/modules.ts`'s header on `assessCalendarEvents` for the full reasoning.

| Calendar entry id           | Target slug                     | Outcome                    |
| --------------------------- | ------------------------------- | -------------------------- |
| `aug-back-to-school`        | `august-2026-activity-calendar` | points-to-migrated-content |
| `aug-mothers-day`           | `august-2026-activity-calendar` | points-to-migrated-content |
| `aug-polsci-orientation`    | `polsci-orientation-2026`       | points-to-migrated-content |
| `aug-thai-peace-day`        | `august-2026-activity-calendar` | points-to-migrated-content |
| `aug-tpc-crazy-week`        | `tpc-crazy-week-2026`           | points-to-migrated-content |
| `aug-tpc-firstmeet`         | `august-2026-activity-calendar` | points-to-migrated-content |
| `aug-tu-freshy-orientation` | `august-2026-activity-calendar` | points-to-migrated-content |
| `ay69-s2-songkran`          | `academic-calendar-2569`        | points-to-migrated-content |
| `jul-newbies`               | `july-2026-activity-calendar`   | points-to-migrated-content |
| `jul-orientation`           | `freshers-orientation-2026`     | points-to-migrated-content |
| `jul-presession`            | `bir18-pre-session`             | points-to-migrated-content |
| `jul-singhadang`            | `singhadang-samphan-2026`       | points-to-migrated-content |
| `jun-faculty-anniversary`   | `faculty-77th-anniversary`      | points-to-migrated-content |
| `jun-recruitment`           | `birsa-committee-recruitment`   | points-to-migrated-content |
| `jun-revolution-day`        | `june-2026-activity-calendar`   | points-to-migrated-content |
| `jun-tu-anniversary`        | `june-2026-activity-calendar`   | points-to-migrated-content |

## Committee (`content/committee.ts` -> `committeeMember`)

| Member key                  | Document id                                  |
| --------------------------- | -------------------------------------------- |
| `anantachai-korkiertsatean` | `committee-member-anantachai-korkiertsatean` |
| `chayapon-srisukho`         | `committee-member-chayapon-srisukho`         |
| `issaree-suwanprapa`        | `committee-member-issaree-suwanprapa`        |
| `janyawat-chaiyaroj`        | `committee-member-janyawat-chaiyaroj`        |
| `jidapa-rattanaburee`       | `committee-member-jidapa-rattanaburee`       |
| `kimitha-saikasin`          | `committee-member-kimitha-saikasin`          |
| `kittiya-kanboonma`         | `committee-member-kittiya-kanboonma`         |
| `kritpol-komkai`            | `committee-member-kritpol-komkai`            |
| `naphut-nilkamhang`         | `committee-member-naphut-nilkamhang`         |
| `nicharee-wutthironprasert` | `committee-member-nicharee-wutthironprasert` |
| `nonnaree-timklay`          | `committee-member-nonnaree-timklay`          |
| `pannawit-binsanorh`        | `committee-member-pannawit-binsanorh`        |
| `phaotip-thipayasothorn`    | `committee-member-phaotip-thipayasothorn`    |
| `pitchaya-singmui`          | `committee-member-pitchaya-singmui`          |
| `pitchayamon-jensirisak`    | `committee-member-pitchayamon-jensirisak`    |
| `preamrudee-yaikla`         | `committee-member-preamrudee-yaikla`         |
| `punsak-ketmalasiri`        | `committee-member-punsak-ketmalasiri`        |
| `sovanpannha-rith`          | `committee-member-sovanpannha-rith`          |
| `sujira-onpocha`            | `committee-member-sujira-onpocha`            |
| `torlap-sonprathed`         | `committee-member-torlap-sonprathed`         |
| `yositar-rouythanapanich`   | `committee-member-yositar-rouythanapanich`   |

## Portfolios (`lib/portfolios.ts` -> `portfolio`, cross-checked against committee.ts)

| Portfolio id           | Outcome                      |
| ---------------------- | ---------------------------- |
| `president`            | not migrated — see gap below |
| `secretariat`          | migrated                     |
| `treasury`             | not migrated — see gap below |
| `spokesperson`         | not migrated — see gap below |
| `public-relations`     | migrated                     |
| `academic-affairs`     | migrated                     |
| `general-coordination` | not migrated — see gap below |
| `sport`                | migrated                     |
| `rights-and-welfare`   | migrated                     |
| `student-activities`   | not migrated — see gap below |
| `foreign-students`     | not migrated — see gap below |
| `merchandise`          | not migrated — see gap below |
| `it-infrastructure`    | not migrated — see gap below |

## Site settings (`content/site.ts` -> `siteSettings` singleton)

`contact` (email, secondaryEmail, phone, address) migrated. `socials` and `officialLinks` not migrated — see gaps.

## Quick links (`content/quick.ts`) — not migrated

All 20 items across 5 groups. See gaps and `lib/migration/modules.ts`'s header on `assessQuickLinks` for why neither `navigation` nor `siteSettings` fits.

## Reporting channels (`content/reporting.ts`) — not migrated

See the single gap below.

## Every gap, by scope

| Scope                                       | Field                                                      | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------------------------- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `committeeMember:anantachai-korkiertsatean` | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:chayapon-srisukho`         | portfolio                                                  | Resolves to portfolio "president", but that portfolio was not migrated (see portfolio:president's own gap: fewer than two current holders). Reference omitted rather than pointing at a document this artifact does not contain.                                                                                                                                                                                                                                                                                                                                                                                                     |
| `committeeMember:chayapon-srisukho`         | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:issaree-suwanprapa`        | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:janyawat-chaiyaroj`        | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:jidapa-rattanaburee`       | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:kimitha-saikasin`          | portfolio                                                  | Resolves to portfolio "treasury", but that portfolio was not migrated (see portfolio:treasury's own gap: fewer than two current holders). Reference omitted rather than pointing at a document this artifact does not contain.                                                                                                                                                                                                                                                                                                                                                                                                       |
| `committeeMember:kimitha-saikasin`          | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:kittiya-kanboonma`         | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:kritpol-komkai`            | portfolio                                                  | Resolves to portfolio "it-infrastructure", but that portfolio was not migrated (see portfolio:it-infrastructure's own gap: fewer than two current holders). Reference omitted rather than pointing at a document this artifact does not contain.                                                                                                                                                                                                                                                                                                                                                                                     |
| `committeeMember:kritpol-komkai`            | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:naphut-nilkamhang`         | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:nicharee-wutthironprasert` | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:nonnaree-timklay`          | portfolio                                                  | Resolves to portfolio "general-coordination", but that portfolio was not migrated (see portfolio:general-coordination's own gap: fewer than two current holders). Reference omitted rather than pointing at a document this artifact does not contain.                                                                                                                                                                                                                                                                                                                                                                               |
| `committeeMember:nonnaree-timklay`          | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:pannawit-binsanorh`        | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:phaotip-thipayasothorn`    | portfolio                                                  | Resolves to portfolio "merchandise", but that portfolio was not migrated (see portfolio:merchandise's own gap: fewer than two current holders). Reference omitted rather than pointing at a document this artifact does not contain.                                                                                                                                                                                                                                                                                                                                                                                                 |
| `committeeMember:phaotip-thipayasothorn`    | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:pitchaya-singmui`          | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:pitchayamon-jensirisak`    | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:preamrudee-yaikla`         | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:punsak-ketmalasiri`        | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:sovanpannha-rith`          | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:sujira-onpocha`            | portfolio                                                  | Resolves to portfolio "spokesperson", but that portfolio was not migrated (see portfolio:spokesperson's own gap: fewer than two current holders). Reference omitted rather than pointing at a document this artifact does not contain.                                                                                                                                                                                                                                                                                                                                                                                               |
| `committeeMember:sujira-onpocha`            | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:torlap-sonprathed`         | portfolio                                                  | Resolves to portfolio "foreign-students", but that portfolio was not migrated (see portfolio:foreign-students's own gap: fewer than two current holders). Reference omitted rather than pointing at a document this artifact does not contain.                                                                                                                                                                                                                                                                                                                                                                                       |
| `committeeMember:torlap-sonprathed`         | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `committeeMember:yositar-rouythanapanich`   | portfolio                                                  | Resolves to portfolio "student-activities", but that portfolio was not migrated (see portfolio:student-activities's own gap: fewer than two current holders). Reference omitted rather than pointing at a document this artifact does not contain.                                                                                                                                                                                                                                                                                                                                                                                   |
| `committeeMember:yositar-rouythanapanich`   | portrait, roleEmail                                        | Both optional. `portrait` needs an uploaded image asset (this migration has no write token and does not touch binary assets — see the shared brief); the header comment on content/committee.ts names the expected file at public/committee/<key>.*, so a follow-up asset-upload pass has what it needs. `roleEmail` has no source: committee.ts carries no email field at all, by the file's own guard (tests/unit/content.test.ts), so none is invented here either.                                                                                                                                                               |
| `portfolio:foreign-students`                | holder, secondHolder                                       | §7.2's two person rule requires a second holder, but content/committee.ts's current roster has only 1 member(s) whose role title matches this portfolio's heldBy list (["Assistant Officer, Foreign Students Assistance"]). This is an operational fact about the real 2026 committee, not a migration bug: BIRSA's current roster does not satisfy the two-person rule for this portfolio. Not migrated; inventing a second holder was not an option.                                                                                                                                                                               |
| `portfolio:general-coordination`            | holder, secondHolder                                       | §7.2's two person rule requires a second holder, but content/committee.ts's current roster has only 1 member(s) whose role title matches this portfolio's heldBy list (["General Coordinator"]). This is an operational fact about the real 2026 committee, not a migration bug: BIRSA's current roster does not satisfy the two-person rule for this portfolio. Not migrated; inventing a second holder was not an option.                                                                                                                                                                                                          |
| `portfolio:it-infrastructure`               | holder, secondHolder                                       | §7.2's two person rule requires a second holder, but content/committee.ts's current roster has only 1 member(s) whose role title matches this portfolio's heldBy list (["Assistant Officer, IT Infrastructure"]). This is an operational fact about the real 2026 committee, not a migration bug: BIRSA's current roster does not satisfy the two-person rule for this portfolio. Not migrated; inventing a second holder was not an option.                                                                                                                                                                                         |
| `portfolio:merchandise`                     | holder, secondHolder                                       | §7.2's two person rule requires a second holder, but content/committee.ts's current roster has only 1 member(s) whose role title matches this portfolio's heldBy list (["Assistant Officer, Merchandise"]). This is an operational fact about the real 2026 committee, not a migration bug: BIRSA's current roster does not satisfy the two-person rule for this portfolio. Not migrated; inventing a second holder was not an option.                                                                                                                                                                                               |
| `portfolio:president`                       | holder, secondHolder                                       | §7.2's two person rule requires a second holder, but content/committee.ts's current roster has only 1 member(s) whose role title matches this portfolio's heldBy list (["President"]). This is an operational fact about the real 2026 committee, not a migration bug: BIRSA's current roster does not satisfy the two-person rule for this portfolio. Not migrated; inventing a second holder was not an option.                                                                                                                                                                                                                    |
| `portfolio:spokesperson`                    | holder, secondHolder                                       | §7.2's two person rule requires a second holder, but content/committee.ts's current roster has only 1 member(s) whose role title matches this portfolio's heldBy list (["Spokesperson"]). This is an operational fact about the real 2026 committee, not a migration bug: BIRSA's current roster does not satisfy the two-person rule for this portfolio. Not migrated; inventing a second holder was not an option.                                                                                                                                                                                                                 |
| `portfolio:student-activities`              | holder, secondHolder                                       | §7.2's two person rule requires a second holder, but content/committee.ts's current roster has only 1 member(s) whose role title matches this portfolio's heldBy list (["Assistant Officer, Student Activities"]). This is an operational fact about the real 2026 committee, not a migration bug: BIRSA's current roster does not satisfy the two-person rule for this portfolio. Not migrated; inventing a second holder was not an option.                                                                                                                                                                                        |
| `portfolio:treasury`                        | holder, secondHolder                                       | §7.2's two person rule requires a second holder, but content/committee.ts's current roster has only 1 member(s) whose role title matches this portfolio's heldBy list (["Treasurer"]). This is an operational fact about the real 2026 committee, not a migration bug: BIRSA's current roster does not satisfy the two-person rule for this portfolio. Not migrated; inventing a second holder was not an option.                                                                                                                                                                                                                    |
| `quick:find-follow/email`                   | (whole item)                                               | Internal path with no document to reference (no route registry exists yet to resolve it); no schema field can hold it as-is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `quick:find-follow/email-secondary`         | (whole item)                                               | Internal path with no document to reference (no route registry exists yet to resolve it); no schema field can hold it as-is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `quick:find-follow/facebook`                | (whole item)                                               | External href; no schema field on any candidate type (navigation.navLink requires an internal path; see this file's header for the full reasoning).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `quick:find-follow/instagram`               | (whole item)                                               | External href; no schema field on any candidate type (navigation.navLink requires an internal path; see this file's header for the full reasoning).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `quick:find-follow/line`                    | (whole item)                                               | Internal path with no document to reference (no route registry exists yet to resolve it); no schema field can hold it as-is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `quick:get-help/contact-birsa`              | (whole item)                                               | Internal path with no document to reference (no route registry exists yet to resolve it); no schema field can hold it as-is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `quick:get-help/emergency-guidance`         | (whole item)                                               | Internal path with no document to reference (no route registry exists yet to resolve it); no schema field can hold it as-is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `quick:get-help/get-an-answer`              | (whole item)                                               | Internal path with no document to reference (no route registry exists yet to resolve it); no schema field can hold it as-is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `quick:get-help/give-feedback`              | (whole item)                                               | Internal path with no document to reference (no route registry exists yet to resolve it); no schema field can hold it as-is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `quick:get-help/student-life-guide`         | (whole item)                                               | Internal path with no document to reference (no route registry exists yet to resolve it); no schema field can hold it as-is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `quick:how-birsa-works/birsa-activity`      | (whole item)                                               | Internal path with no document to reference (no route registry exists yet to resolve it); no schema field can hold it as-is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `quick:official/bir-program`                | (whole item)                                               | External href; no schema field on any candidate type (navigation.navLink requires an internal path; see this file's header for the full reasoning).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `quick:official/faculty`                    | (whole item)                                               | External href; no schema field on any candidate type (navigation.navLink requires an internal path; see this file's header for the full reasoning).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `quick:top-tasks/borrow-equipment`          | (whole item)                                               | Internal path with no document to reference (no route registry exists yet to resolve it); no schema field can hold it as-is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `quick:top-tasks/course-reviews`            | (whole item)                                               | Internal path with no document to reference (no route registry exists yet to resolve it); no schema field can hold it as-is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `quick:top-tasks/find-a-club`               | (whole item)                                               | Internal path with no document to reference (no route registry exists yet to resolve it); no schema field can hold it as-is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `quick:top-tasks/getting-started`           | (whole item)                                               | Internal path with no document to reference (no route registry exists yet to resolve it); no schema field can hold it as-is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `quick:top-tasks/internship`                | (whole item)                                               | Internal path with no document to reference (no route registry exists yet to resolve it); no schema field can hold it as-is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `quick:top-tasks/shuttle-bus`               | (whole item)                                               | Internal path with no document to reference (no route registry exists yet to resolve it); no schema field can hold it as-is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `quick:top-tasks/whats-on`                  | (whole item)                                               | Internal path with no document to reference (no route registry exists yet to resolve it); no schema field can hold it as-is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `regulation:discipline-2568`                | citation, authority, preamble, signatory                   | RegulationMeta carries all four (the full citation, the issuing authority, the recital preamble, and the signatory block). regulation.ts's schema has no field for any of them: its only fields are title, slug, body, effectiveDate, supersedes and lifecycle.                                                                                                                                                                                                                                                                                                                                                                      |
| `regulation:discipline-2568`                | lifecycle.owner                                            | Required on every document, but which BIRSA portfolio owns a regulation is a governance decision this migration has no source data for (unlike a committee member's portfolio, which is derivable from lib/portfolios.ts's heldBy list, a regulation names no owning portfolio anywhere in its own data). An officer or developer must set this before the stub can leave draft.                                                                                                                                                                                                                                                     |
| `regulation:discipline-2568`                | sections (the whole provision tree)                        | 6 section group(s), 41 provision(s) and 51 provision item(s)/definition(s), bilingual, with legally significant numbering, explicit item markers and clause nesting. regulation.ts's only content field is a single-locale portableText array (h2/h3 only, renderer-numbered lists). Flattening this tree into it would either collapse the Title/Chapter/Division hierarchy into two heading levels, or drop one language, or both — exactly the loss the brief forbids. Not migrated; the 1.0 route keeps rendering this content from the TypeScript module.                                                                       |
| `regulation:political-science-2565`         | citation, authority, preamble, signatory                   | RegulationMeta carries all four (the full citation, the issuing authority, the recital preamble, and the signatory block). regulation.ts's schema has no field for any of them: its only fields are title, slug, body, effectiveDate, supersedes and lifecycle.                                                                                                                                                                                                                                                                                                                                                                      |
| `regulation:political-science-2565`         | lifecycle.owner                                            | Required on every document, but which BIRSA portfolio owns a regulation is a governance decision this migration has no source data for (unlike a committee member's portfolio, which is derivable from lib/portfolios.ts's heldBy list, a regulation names no owning portfolio anywhere in its own data). An officer or developer must set this before the stub can leave draft.                                                                                                                                                                                                                                                     |
| `regulation:political-science-2565`         | sections (the whole provision tree)                        | 13 section group(s), 89 provision(s) and 150 provision item(s)/definition(s), bilingual, with legally significant numbering, explicit item markers and clause nesting. regulation.ts's only content field is a single-locale portableText array (h2/h3 only, renderer-numbered lists). Flattening this tree into it would either collapse the Title/Chapter/Division hierarchy into two heading levels, or drop one language, or both — exactly the loss the brief forbids. Not migrated; the 1.0 route keeps rendering this content from the TypeScript module.                                                                     |
| `regulation:university-2563`                | citation, authority, preamble, signatory                   | RegulationMeta carries all four (the full citation, the issuing authority, the recital preamble, and the signatory block). regulation.ts's schema has no field for any of them: its only fields are title, slug, body, effectiveDate, supersedes and lifecycle.                                                                                                                                                                                                                                                                                                                                                                      |
| `regulation:university-2563`                | lifecycle.owner                                            | Required on every document, but which BIRSA portfolio owns a regulation is a governance decision this migration has no source data for (unlike a committee member's portfolio, which is derivable from lib/portfolios.ts's heldBy list, a regulation names no owning portfolio anywhere in its own data). An officer or developer must set this before the stub can leave draft.                                                                                                                                                                                                                                                     |
| `regulation:university-2563`                | sections (the whole provision tree)                        | 35 section group(s), 96 provision(s) and 225 provision item(s)/definition(s), bilingual, with legally significant numbering, explicit item markers and clause nesting. regulation.ts's only content field is a single-locale portableText array (h2/h3 only, renderer-numbered lists). Flattening this tree into it would either collapse the Title/Chapter/Division hierarchy into two heading levels, or drop one language, or both — exactly the loss the brief forbids. Not migrated; the 1.0 route keeps rendering this content from the TypeScript module.                                                                     |
| `reporting`                                 | reportingChannels, reportingCopy (whole module)            | Two named contact channels (organisation, person, phone, phone href, optional extension, email) plus their surrounding copy. Not a serviceDefinition (no start page or question flow) and not siteSettings.contactRoute (that type routes a category to a BIRSA portfolio id; one channel here, the BIR Programme office, is not a BIRSA portfolio at all, and neither channel's shape — organisation/person/phone/extension/email — has a home in contactRoute's two fields). No document type in the current schema fits. Not service configuration either: it is read-facing safety content, just content with nowhere to go yet. |
| `siteSettings`                              | lifecycle.owner                                            | Required by the schema. content/site.ts names no owning portfolio for this configuration (it predates the portfolio/ownership model entirely); an officer or developer must choose one before this document can leave draft.                                                                                                                                                                                                                                                                                                                                                                                                         |
| `siteSettings`                              | officialLinks (birProgram, faculty, registrar, university) | These are external URLs (https://polsci.tu.ac.th, etc.). siteSettings.ts has no field for them, and navigation.ts's navLink type could not hold them either even if it were the right place: navLink.href is validated to start with "/", i.e. internal paths only (sanity/schemaTypes/documents/navigation.ts). Not migrated.                                                                                                                                                                                                                                                                                                       |
| `siteSettings`                              | socials (instagram, facebook, email, line)                 | content/site.ts's `socials` array (with each channel's href and a placeholder flag for LINE) has no field anywhere in siteSettings.ts. Not migrated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

## Unaccounted for

None. Every file under `content/activity/regulations/**`, plus `content/calendar/events.ts`, `content/committee.ts`, `content/site.ts`, `content/quick.ts`, `content/reporting.ts` and `lib/portfolios.ts`, has an outcome above (migrated, migrated-with-gaps, or not migrated with a reason).
